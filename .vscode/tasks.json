{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/watson-glaser-trainer/Dockerfile",
				"context": "${workspaceFolder}/watson-glaser-trainer",
				"pull": true
			},
			"node": {
				"package": "${workspaceFolder}/watson-glaser-trainer/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: release",
			"dependsOn": [
				"docker-build"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/watson-glaser-trainer/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"DEBUG": "*",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/watson-glaser-trainer/package.json",
				"enableDebugging": true
			}
		}
	]
}