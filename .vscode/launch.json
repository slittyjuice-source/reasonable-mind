{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/watson-glaser-trainer/package.json",
                "localRoot": "${workspaceFolder}/watson-glaser-trainer"
            }
        }
    ]
}